// ThemeService was used for a user-selectable dark mode toggle.
// Dark mode has been removed per project direction; this file is retained
// as a harmless placeholder in case the feature is reintroduced later.

// (No runtime code required.)

/*
If you want to fully remove this file, delete it from
lib/services/theme_service.dart. For now it is intentionally left
in the repo as a comment to avoid accidental imports causing build
errors while removing the runtime toggle.
*/
